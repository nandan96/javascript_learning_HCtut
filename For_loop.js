let days = ['mon','tue','wed','thu','fri','sat','sun'] //defining a array
let index = 0                           //defining a index value 
for (index;index<days.length;index++){  // for (1time before the code block exe; condition ; everytime after code block exe)
    console.log(days[index])
}
console.log("\nbye")

// for (index = days.length;index>=0;index--){  //to show the reverse op of the arrary
//     console.log(days[index])
// }